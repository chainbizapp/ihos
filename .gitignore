# .NET / C#
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/
packages/

# C# Dev Kit / Roslyn language server cache
*.lscache
*.lscache.v*
appsettings.Development.json
appsettings.*.json
!appsettings.json
!appsettings.Development.json.example

# Node.js / Angular
frontend/node_modules/
frontend/dist/
frontend/.angular/
frontend/.cache/
*.log
npm-debug.log*

# Environment & secrets
.env
.env.*
!.env.example
*.key
*.pem
*.pfx

# Docker
docker/postgres/data/

# JetBrains / VSCode
.idea/
.vscode/
*.iml

# macOS
.DS_Store
Thumbs.db

# Build outputs
*.tmp
*.swp
coverage/
TestResults/

# Reports (user-generated, not templates)
reports/output/
.deploy_build/

# ---- Claude / design reference (kept on disk, NOT version-controlled) ----
# Visual references exported from Figma — regenerable, so not tracked.
wireframes/
# Cross-session handoff & prompt notes (local working docs, not app code).
PROJECT_HANDOFF.md
FRONTEND_DEV_HANDOFF.md
SCREEN_PROMPTS.md
# Figma plugins (tooling, not part of the app build) — uncomment to ignore:
# figma-plugin-rename-roles/
# figma-plugin-en-to-th/
